{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Start TPC server",
            "program": "${workspaceFolder}/backend/app.js",
            "env": {
                "PORT": "3000"
            }
        }
    ]
}
